// we can take string using scanf and print it using print f.//
#include<stdio.h>
int main()
{
    char str[40];
    printf(" Enter a string %s", str);
    scanf("%s",str);
    printf(" your string is %s",str);
    return 0;
}
